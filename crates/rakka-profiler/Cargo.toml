[package]
name = "rakka-profiler"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Cross-runtime actor profiler for rakka — same scenarios in Rust and Python with a shared JSON schema."
homepage.workspace = true
readme = "../../README.md"
documentation = "https://docs.rs/rakka-profiler"
keywords = ["actor", "profiler", "benchmark"]
categories = ["development-tools::profiling", "concurrency"]

[lib]
path = "src/lib.rs"

[[bin]]
name = "rakka-profiler"
path = "src/bin/rakka_profiler.rs"

[dependencies]
rakka = { package = "rakka-rs", path = "../rakka", version = "0.2.0" }
rakka-core = { workspace = true }
tokio = { workspace = true }
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
